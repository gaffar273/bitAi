# Server
PORT=5000
NODE_ENV=development

# Yellow Network (ClearNode)
# Sandbox (no real tokens needed - for testing):
YELLOW_WS_URL=wss://clearnet-sandbox.yellow.com/ws

# Optional: Private key for server-side signing (demo/testing only)
# In production, frontend connects user wallet and signs client-side
# PRIVATE_KEY=your_private_key_here

# Note: Yellow state channels = NO GAS for off-chain transactions
# Gas only paid when opening/settling channels (on Base L2)
# Sandbox mode = no real tokens needed

# USDC amounts use 6 decimals (micro-USDC)
# 1000000 = 1.00 USDC
# 100000 = 0.10 USDC
# 50000 = 0.05 USDC
